{
  "name": "moviecutter-e2e",
  "private": true,
  "description": "Playwright E2E tests against the production Firebase Hosting site",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
